from django.db import models

from order.models import Order

# Create your models here.

# class MpesaPayment(models.Model):
#     order_transaction_id = models.ForeignKey(Order.transaction_id)
#     amount = models.ForeignKey(Order.order_total)
#     phone_number = models.ForeignKey(Order.)


